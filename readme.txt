To run this file, you just need to configure lastSession.txt file. write the value 0 to scrape from starting.

If you stop script in between, it will save the session and resume from the same category.

DONT EDIT lastSession.txt AFTER INITIALIZATION TO 0, DATA CAN BE OMITTED.